# if else example

a = int(input('Give an integer: '))
b = int(input('and another one: '))

if a == b:
    print('Numbers are equal')
else:
    if a > b:
        max = a
        min = b
    else:
        max = b
        min = a

    print(max, 'is greater than', min)

    print('%d is greater' % a if a > b else a)
    # <command1> <if> <expression> <else> <command2>
