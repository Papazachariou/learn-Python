# surface area of a cone

import math

pi = math.pi

radius = float(input('Enter the radius: '))

height = float(input('Enter the height: '))

coneArea = pi * radius * (math.sqrt(radius ** 2 + height ** 2) + radius)

print('The area of the cone is %.2f' % coneArea)
